apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: studio-shikanime-i-receivers-flux
  namespace: flux-system
spec:
  commonName: receivers.flux.i.shikanime.studio
  dnsNames:
    - receivers.flux.i.shikanime.studio
  issuerRef:
    name: studio-shikanime
    kind: ClusterIssuer
  secretName: studio-shikanime-i-receivers-flux
  usages:
    - digital signature
    - key encipherment
    - server auth
