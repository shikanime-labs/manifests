---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flux-operator-receivers
  namespace: flux-system
spec:
  parentRefs:
    - name: flux-operator-receivers
      sectionName: https
  hostnames:
    - receivers.flux.i.shikanime.studio
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flux-operator-receivers-redirect
  namespace: flux-system
spec:
  parentRefs:
    - name: flux-operator-receivers
      sectionName: http
  hostnames:
    - receivers.flux.i.shikanime.studio
