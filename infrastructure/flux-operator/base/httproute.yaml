apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flux-operator
  namespace: flux-system
spec:
  parentRefs:
    - name: flux-operator
      sectionName: https
  hostnames:
    - flux.i.shikanime.studio
  rules:
    - backendRefs:
        - name: flux-operator
          port: 9080
