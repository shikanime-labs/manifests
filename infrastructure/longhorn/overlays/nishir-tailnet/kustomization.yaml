resources:
  - ../nishir
  - gatewayclass.yaml
  - envoyproxy.yaml
  - httproute.yaml
  - cert.yaml
patches:
  - path: patch-hr.yaml
    target:
      name: longhorn
      version: v2
      kind: HelmRelease
