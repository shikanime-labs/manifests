apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vpa-tls-secret
  namespace: autoscaler-system
spec:
  commonName: vpa-webhook
  dnsNames:
    - vpa-webhook
    - vpa-webhook.autoscaler-system.svc.cluster.local
  duration: 8760h
  issuerRef:
    name: nishir
    kind: ClusterIssuer
  renewBefore: 720h
  secretName: vpa-tls-secret
  usages:
    - digital signature
    - key encipherment
    - server auth
