apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: leaderworkerset
  namespace: flux-system
spec:
  chart:
    spec:
      version: 0.6.0
      chart: leaderworkerset
      interval: 1h
      sourceRef:
        name: leaderworkerset
        kind: HelmRepository
        namespace: flux-system
  interval: 10m
  releaseName: leaderworkerset
  targetNamespace: leaderworkerset-system
  values: {}
