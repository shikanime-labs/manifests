apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vpa
  namespace: flux-system
spec:
  values:
    admissionController:
      certManager:
        issuerRef:
          name: nishir
          kind: ClusterIssuer
