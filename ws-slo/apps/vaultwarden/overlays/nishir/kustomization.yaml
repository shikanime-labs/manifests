resources:
  - ../../base
  - backendtlspolicy.yaml
  - cert.yaml
  - gateway.yaml
namespace: shikanime
components:
  - ../../components/tls
patches:
  - patch: |-
      - op: add
        path: /spec/hostnames
        value:
        - vaultwarden.i.shikanime.studio
      - op: add
        path: /spec/parentRefs
        value:
        - name: vaultwarden
          sectionName: https
    target:
      name: vaultwarden
      kind: HTTPRoute
  - patch: |-
      - op: add
        path: /spec/hostnames
        value:
        - vaultwarden.i.shikanime.studio
      - op: add
        path: /spec/parentRefs
        value:
        - name: vaultwarden
          sectionName: http
    target:
      name: vaultwarden-redirect
      kind: HTTPRoute
secretGenerator:
  - name: vaultwarden-env
    envs:
      - vaultwarden-env/.enc.env
