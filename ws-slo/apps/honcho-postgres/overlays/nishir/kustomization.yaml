resources:
  - ../../base
namespace: shikanime
patches:
secretGenerator:
  - name: honcho-postgres-env
    type: basic-auth
    envs:
      - honcho-postgres-env/.enc.env
    options:
      disableNameSuffixHash: true
