apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: telsha-webhook
  namespace: flux-system
spec:
  parentRefs:
    - name: telsha-webhook
      sectionName: https
  rules:
    - backendRefs:
        - name: webhook-receiver
          port: 80
