apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-lws
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  deletionPolicy: Orphan
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: leaderworkerset
      namespace: flux-system
  interval: 10m
  path: ./infrastructure/lws/overlays/nishir
  prune: true
  sourceRef:
    name: flux-system
    kind: GitRepository
