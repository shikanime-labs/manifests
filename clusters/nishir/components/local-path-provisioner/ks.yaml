apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-local-path-provisioner
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  deletionPolicy: Orphan
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: local-path-provisioner
      namespace: flux-system
  interval: 10m
  path: ./infrastructure/local-path-provisioner/overlays/nishir
  prune: true
  sourceRef:
    name: flux-system
    kind: GitRepository
