apiVersion: v1
kind: Service
metadata:
  name: lldap
spec:
  clusterIP: None
  ports:
    - name: ldap
      port: 3890
      targetPort: ldap
  selector:
    app.kubernetes.io/instance: lldap
    app.kubernetes.io/name: lldap
