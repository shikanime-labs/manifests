apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: lldap
spec:
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        resources:
          requests:
            storage: 512Mi
        volumeMode: Filesystem
        accessModes:
          - ReadWriteOncePod
        storageClassName: nishir-standard
