apiVersion: v1
kind: Service
metadata:
  name: vllm-deepseek
  namespace: shikanime
spec:
  selector:
    app.kubernetes.io/instance: vllm-deepseek
    app.kubernetes.io/component: ray-head
  ports:
    - name: http
      port: 8000
      targetPort: http
