apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: signal-redirect
spec:
  hostnames:
      - signal.i.shikanime.studio
  parentRefs:
    - name: signal
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
