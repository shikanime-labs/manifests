apiVersion: v1
kind: Service
metadata:
  name: ds4-coordinator
spec:
  selector:
    app.kubernetes.io/instance: ds4-coordinator
    app.kubernetes.io/name: ds4-coordinator
  ports:
    - name: http
      appProtocol: http
      port: 8000
      protocol: TCP
      targetPort: http
