resources:
  - ../../base
namespace: shikanime
patches:
  - path: patch-pvc.yaml
secretGenerator:
  - name: immich-postgres
    type: basic-auth
    envs:
      - immich-postgres/.enc.env
    options:
      disableNameSuffixHash: true
