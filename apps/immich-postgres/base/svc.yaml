apiVersion: v1
kind: Service
metadata:
  name: immich-postgres
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app.kubernetes.io/instance: immich-postgres
    app.kubernetes.io/name: postgres
