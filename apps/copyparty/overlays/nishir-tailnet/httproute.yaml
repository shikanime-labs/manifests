apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: copyparty
spec:
  hostnames:
    - copyparty.i.shikanime.studio
  parentRefs:
    - name: copyparty
      sectionName: https
  rules:
    - backendRefs:
        - name: copyparty
          port: http
