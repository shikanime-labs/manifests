apiVersion: v1
kind: Service
metadata:
  name: gitea-mirror
spec:
  sessionAffinity: ClientIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/instance: gitea-mirror
    app.kubernetes.io/name: gitea-mirror
