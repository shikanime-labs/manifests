resources:
  - ../../base
  - gateway.yaml
namespace: shikanime
patches:
  - path: patch-pvc.yaml
  - patch: |-
      - op: add
        path: /spec/hostnames
        value:
        - gitea-mirror.i.shikanime.studio
      - op: add
        path: /spec/parentRefs
        value:
        - name: gitea-mirror
          sectionName: https
    target:
      kind: HTTPRoute
      name: gitea-mirror
  - patch: |-
      - op: add
        path: /spec/hostnames
        value:
        - gitea-mirror.i.shikanime.studio
      - op: add
        path: /spec/parentRefs
        value:
        - name: gitea-mirror
          sectionName: http
    target:
      kind: HTTPRoute
      name: gitea-mirror-redirect
