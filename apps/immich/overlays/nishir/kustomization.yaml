resources:
  - ../../base
namespace: shikanime
patches:
  - path: patch-deploy.yaml
  - path: patch-pvc.yaml
secretGenerator:
  - name: immich
    envs:
      - immich/.enc.env
    options:
      disableNameSuffixHash: true
