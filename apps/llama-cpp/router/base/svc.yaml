apiVersion: v1
kind: Service
metadata:
  name: inference
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/instance: inference
    app.kubernetes.io/name: inference
