apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deepseek-flash
spec:
  defaultBackend:
    service:
      name: deepseek-flash
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - deepseek-flash
