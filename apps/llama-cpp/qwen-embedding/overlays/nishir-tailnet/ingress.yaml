apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qwen-embedding
spec:
  defaultBackend:
    service:
      name: qwen-embedding
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - qwen-embedding
