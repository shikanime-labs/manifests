apiVersion: v1
kind: Service
metadata:
  name: qwen-embedding
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/instance: qwen-embedding
    app.kubernetes.io/name: qwen-embedding
