apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: inference
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: inference
      app.kubernetes.io/name: inference
  policyTypes:
    - Ingress
