apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - cert.yaml
  - clienttrafficpolicy.yaml
patches:
  - path: patch-gateway.yaml
    target:
      name: inference
      kind: Gateway
