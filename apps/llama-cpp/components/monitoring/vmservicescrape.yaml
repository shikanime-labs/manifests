apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: llama-cpp
  labels:
    app.kubernetes.io/name: llama-cpp
    app.kubernetes.io/part-of: nishir-inference
spec:
  endpoints:
    - interval: 60s
      path: /metrics
      params:
        model:
          - qwen/qwen3.8-27b
      port: http
      scrapeTimeout: 30s
      relabelConfigs:
        - sourceLabels: [__meta_kubernetes_service_name]
          regex: llama-cpp-headless
          action: drop
        - targetLabel: model
          replacement: qwen/qwen3.8-27b
        - targetLabel: instance
          replacement: llama-cpp-qwen3.8-27b
    - interval: 2h
      path: /metrics
      params:
        model:
          - qwen/qwen3-embedding-8b
      port: http
      scrapeTimeout: 30s
      relabelConfigs:
        - sourceLabels: [__meta_kubernetes_service_name]
          regex: llama-cpp-headless
          action: drop
        - targetLabel: model
          replacement: qwen/qwen3-embedding-8b
        - targetLabel: instance
          replacement: llama-cpp-qwen3-embedding-8b
  namespaceSelector:
    matchNames:
      - shikanime
  selector:
    matchLabels:
      app.kubernetes.io/instance: llama-cpp
      app.kubernetes.io/name: llama-cpp
