apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: llama-cpp
spec:
  template:
    spec:
      initContainers:
        - name: model-prefetch
          envFrom:
            - secretRef:
                name: llama-cpp-env
