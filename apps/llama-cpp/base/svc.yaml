apiVersion: v1
kind: Service
metadata:
  name: llama-cpp
  namespace: shikanime
spec:
  selector:
    app.kubernetes.io/instance: llama-cpp
    app.kubernetes.io/name: llama-cpp
  ports:
    - name: http
      appProtocol: https
      port: 8080
      protocol: TCP
      targetPort: http
