apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dex-redirect
spec:
  hostnames:
    - dex.i.shikanime.studio
  parentRefs:
    - name: dex
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
