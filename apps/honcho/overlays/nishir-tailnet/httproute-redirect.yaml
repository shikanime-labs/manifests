apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: honcho-redirect
spec:
  hostnames:
      - honcho.i.shikanime.studio
  parentRefs:
    - name: honcho
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
