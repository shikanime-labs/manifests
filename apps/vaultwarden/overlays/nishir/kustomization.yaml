resources:
  - ../../base
  - backendtlspolicy.yaml
  - cert.yaml
  - gateway.yaml
namespace: shikanime
components:
  - ../../components/tls
patches:
  - path: patch-pvc.yaml
  - patch: |-
      - op: add
        path: /spec/hostnames
        value:
        - vaultwarden.i.shikanime.studio
      - op: add
        path: /spec/parentRefs
        value:
        - name: vaultwarden
          sectionName: https
    target:
      name: vaultwarden
      kind: HTTPRoute
  - patch: |-
      - op: add
        path: /spec/hostnames
        value:
        - vaultwarden.i.shikanime.studio
      - op: add
        path: /spec/parentRefs
        value:
        - name: vaultwarden
          sectionName: http
    target:
      name: vaultwarden-redirect
      kind: HTTPRoute
secretGenerator:
  - name: vaultwarden
    envs:
      - vaultwarden/.enc.env
