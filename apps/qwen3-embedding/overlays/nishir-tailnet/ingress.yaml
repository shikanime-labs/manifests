apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qwen3-embedding
spec:
  defaultBackend:
    service:
      name: qwen3-embedding
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - qwen3-embedding
