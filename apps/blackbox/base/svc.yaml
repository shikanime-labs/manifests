apiVersion: v1
kind: Service
metadata:
  name: blackbox
  labels:
    app.kubernetes.io/name: blackbox
spec:
  ports:
    - name: http
      appProtocol: http
      port: 9115
      targetPort: http
  selector:
    app.kubernetes.io/name: blackbox
