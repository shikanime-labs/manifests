apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: blackbox
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: blackbox
      app.kubernetes.io/name: blackbox
  policyTypes:
    - Ingress
  ingress: []
