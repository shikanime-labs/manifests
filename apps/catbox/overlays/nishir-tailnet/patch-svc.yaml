apiVersion: v1
kind: Service
metadata:
  name: catbox
  annotations:
    tailscale.com/hostname: catbox
    tailscale.com/tags: tag:automata,tag:machine,tag:workstation
spec:
  loadBalancerClass: tailscale
