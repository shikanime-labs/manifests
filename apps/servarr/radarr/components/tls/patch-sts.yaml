apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: radarr
spec:
  template:
    spec:
      containers:
        - name: radarr
          livenessProbe:
            httpGet:
              port: https
              scheme: HTTPS
          ports:
            - name: https
              containerPort: 9898
          readinessProbe:
            httpGet:
              port: https
              scheme: HTTPS
          startupProbe:
            httpGet:
              port: https
              scheme: HTTPS
          volumeMounts:
            - name: tls
              mountPath: /etc/ssl/private
              readOnly: true
          env:
            - name: RADARR__SERVER__ENABLESSL
              value: "True"
            - name: RADARR__SERVER__SSLCERTPATH
              value: /etc/ssl/private/keystore.p12
            - name: RADARR__SERVER__SSLCERTPASSWORD
              valueFrom:
                secretKeyRef:
                  name: radarr-env
                  key: ssl-cert-password
            - name: RADARR__SERVER__SSLPORT
              value: "9898"
      volumes:
        - name: tls
          secret:
            items:
              - key: keystore.p12
                mode: 416
                path: keystore.p12
            secretName: radarr-tls
