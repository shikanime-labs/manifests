apiVersion: v1
kind: Service
metadata:
  name: whisparr
spec:
  sessionAffinity: ClientIP
  ports:
    - name: http
      port: 6969
      targetPort: http
  selector:
    app.kubernetes.io/instance: whisparr
    app.kubernetes.io/name: whisparr
