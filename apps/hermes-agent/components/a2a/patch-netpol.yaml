- op: add
  path: /spec/ingress/-
  value:
    from:
      - podSelector:
          matchLabels:
            app.kubernetes.io/part-of: nishir-automata
            app.kubernetes.io/name: a2a
    ports:
      - port: a2a
      - port: api-server
