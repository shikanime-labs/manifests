apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: hermes-agent
spec:
  template:
    spec:
      containers:
        - name: hermes-agent
          env:
            - name: HERMES_DASHBOARD
              value: "1"
          ports:
            - name: dashboard
              containerPort: 9119
