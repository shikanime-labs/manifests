apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: hermes-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: hermes-agent
      app.kubernetes.io/name: hermes-agent
  serviceName: hermes-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: hermes-agent
        app.kubernetes.io/name: hermes-agent
    spec:
      containers:
        - name: hermes-agent
          image: hermes-agent
          args:
            - gateway
            - run
            - --replace
          env:
            - name: DISCORD_ALLOW_BOTS
              value: mentions
            - name: S6_READ_ONLY_ROOT
              value: "1"
          volumeMounts:
            - name: data
              mountPath: /opt/data
            - name: rclone-config
              mountPath: /config/rclone/rclone.conf
              readOnly: true
              subPath: rclone.conf
            - name: run
              mountPath: /run
            - name: ssh-key
              mountPath: /root/.ssh
              readOnly: true
          envFrom:
            - secretRef:
                name: hermes-agent
          securityContext:
            capabilities:
              add:
                - CHOWN
                - DAC_OVERRIDE
                - FOWNER
                - SETGID
                - SETUID
          lifecycle:
            preStop:
              exec:
                command:
                  - hermes
                  - gateway
                  - stop
                  - --all
      initContainers:
        - name: config-sync
          image: rclone
          args:
            - copy
            - --copy-links
            - /mnt/startup-config
            - /opt/data
          volumeMounts:
            - name: cache
              mountPath: /root/.cache
            - name: data
              mountPath: /opt/data
            - name: rclone-config
              mountPath: /config/rclone/rclone.conf
              readOnly: true
              subPath: rclone.conf
            - name: startup-config
              mountPath: /mnt/startup-config
              readOnly: true
          securityContext:
            capabilities:
              add:
                - FOWNER
      securityContext:
        fsGroup: 10000
      volumes:
        - name: cache
          emptyDir:
            medium: Memory
        - name: data
          persistentVolumeClaim:
            claimName: hermes-agent-data
        - name: rclone-config
          configMap:
            name: hermes-agent-rclone
        - name: run
          emptyDir:
            medium: Memory
        - name: startup-config
          projected:
            sources:
              - secret:
                  name: hermes-agent-startup-config
              - configMap:
                  name: hermes-agent-dreamer
                  items:
                    - key: config.yaml
                      path: profiles/dreamer/config.yaml
                    - key: SOUL.md
                      path: profiles/dreamer/SOUL.md
              - configMap:
                  name: hermes-agent-operator
                  items:
                    - key: config.yaml
                      path: profiles/operator/config.yaml
                    - key: SOUL.md
                      path: profiles/operator/SOUL.md
              - configMap:
                  name: hermes-agent-coder
                  items:
                    - key: config.yaml
                      path: profiles/coder/config.yaml
                    - key: SOUL.md
                      path: profiles/coder/SOUL.md
              - configMap:
                  name: hermes-agent-auxiliary
                  items:
                    - key: config.yaml
                      path: profiles/auxiliary/config.yaml
                    - key: SOUL.md
                      path: profiles/auxiliary/SOUL.md
        - name: ssh-key
          secret:
            defaultMode: 400
            secretName: hermes-agent-ssh
