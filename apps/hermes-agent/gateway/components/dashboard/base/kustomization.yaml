apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sts.yaml
  - svc.yaml
  - hpa.yaml
  - vpa.yaml
images:
  - name: hermes-agent
    newName: nousresearch/hermes-agent
    newTag: v2026.6.5
metadata:
  annotations:
    automata.shikanime.studio/images:
      '[ { "name": "hermes-agent", "tag-regex": "^(?P<version>.+)$" } ]'
